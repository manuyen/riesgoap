# Riesgo Aportantes — Streamlit
App de inferencia para clasificar nivel de riesgo (BAJO/ALTO) con pipeline scikit-learn + CatBoost.

**Cómo correr local:**
```bash
pip install -r requirements.txt
streamlit run streamlit_app.py
